version: '3'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://miksystem:@localhost:5432
      - RAILS_ENV=production